language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "pypy"
install:
  - pip install -r requirements.txt
  - pip install .
script:
  - py.test --doctest-modules --pep8 pytzdb -v --cov pytzdb --cov-report term-missing
after_success:
  - coveralls