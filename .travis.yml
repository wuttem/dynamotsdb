language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "pypy"
install:
  - pip install -r requirements.txt
  - pip install .
script:
  - coverage run --source pytsdb -m py.test
after_success:
  - coveralls